##### Clone

```bash
git clone https://github.com/camabeh/openshift-cluster.git --recursive
```

##### Setup

1. Setup proxy in 
    * `hosts.local.yaml`
    * `Vagrantfile`

##### Run

```bash
bash ./deploy.local.sh
```